import React from "react";
import { View, Text, StyleSheet } from "react-native";

interface HelpScreenProps {}

export const HelpScreen: React.FC<HelpScreenProps> = ({}) => {
  return (
    <View>
      <Text>HelpScreenScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
